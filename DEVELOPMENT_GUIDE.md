# English Learning Server 开发指南

## 1. 文档定位

本文档是 `english-learning-server` 重写和后续开发的总体指导文件。

它用于约束项目长期开发方式，包括职责边界、技术栈、代码分层、功能规划流程和质量门禁。本文档不承载某个具体功能的详细实现方案；注册登录、非 AI 学习功能、AI agents 接入等功能都需要在开发前单独编写 feature plan。

## 2. 项目职责

`english-learning-server` 是英语学习产品的后端服务，主要承担以下职责：

1. 用户与认证
   - 用户注册、登录、当前用户信息、密码管理。
   - 第三方登录与本地用户身份绑定。
   - 用户状态、鉴权和用户数据隔离。

2. 非 AI 学习功能
   - 词典查询、翻译、单词详情聚合。
   - 用户词本、学习记录、复习相关数据。
   - 其它不依赖 AI agents 的学习业务能力。

3. AI agents 接入边界
   - server 只负责调用、编排和保护 agents 能力。
   - 具体 agent 能力、提示词、模型调用和评测逻辑不在 server 内实现。
   - agents 项目稳定后，再为具体接入能力单独编写 feature plan。

## 3. 核心技术栈

后端重写默认采用以下技术栈：

- Web 框架：FastAPI
- 数据库：PostgreSQL
- ORM：SQLAlchemy 2 async
- 数据库迁移：Alembic
- 配置管理：pydantic-settings
- 数据校验与序列化：pydantic
- 测试框架：pytest
- 认证基础设施：FastAPI Users 作为默认方向

除非 feature plan 明确说明并经过确认，后续功能开发不应重新选择核心框架、数据库、ORM、迁移工具或测试框架。

## 4. 代码分层

新后端代码应保持清晰分层。每一层只承担自己的职责，避免把 HTTP、数据库、业务流程和第三方调用混在一起。

建议目录职责如下：

- `api`
  - 定义 FastAPI 路由、依赖注入、HTTP 状态码和请求响应边界。
  - 不直接写复杂业务逻辑。

- `schemas`
  - 定义对外 API 的请求体、响应体和公共响应结构。
  - 只表达外部 I/O，不直接等同数据库模型。

- `models`
  - 定义 SQLAlchemy 数据库模型。
  - 数据库字段、索引、约束和关系应与 Alembic migration 保持一致。

- `repositories`
  - 封装数据库读写。
  - 不处理 HTTP 语义，不直接调用第三方服务。

- `services`
  - 承载业务流程和用例编排。
  - 可以组合 repositories、integrations 和领域规则。

- `integrations`
  - 封装第三方服务和跨项目调用。
  - 例如 OIDC provider、词典 API、翻译 API、agents 服务客户端。
  - 必须便于 mock 和测试。

- `core`
  - 承载配置、错误、日志、安全、公共依赖等基础设施。
  - 不放具体业务功能。

## 5. API 约定

所有业务 API 默认使用 `/api/v1` 前缀。

响应结构统一为：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

约定：

- 成功响应 `code` 为 `0`。
- 错误响应必须包含稳定的 `code` 和可读的 `message`。
- `data` 在没有返回数据时可以为 `null`。
- API 字段命名默认使用 `snake_case`。
- 修改公开 API 前，必须在对应 feature plan 中说明兼容性影响。

## 6. 功能开发流程

每个业务功能开发前必须先编写单独的 feature plan。

feature plan 至少包含：

1. 目标
   - 该功能要解决什么问题。
   - 面向哪个使用场景或调用方。

2. 非目标
   - 明确本次不做什么。
   - 避免在实现时扩大范围。

3. API 设计
   - 路径、方法、请求体、响应体、错误响应。
   - 是否需要鉴权。
   - 是否影响已有调用方。

4. 数据模型与迁移
   - 新增或修改的表、字段、索引、约束。
   - Alembic migration 策略。
   - 是否需要兼容旧数据。

5. 服务流程
   - 主要业务流程。
   - 与 repository、integration、外部服务的交互。

6. 错误处理
   - 参数错误。
   - 鉴权失败。
   - 权限不足。
   - 第三方服务失败。
   - 数据不存在或状态不合法。

7. 测试场景
   - 成功路径。
   - 关键失败路径。
   - 用户数据隔离。
   - 外部服务 mock。

8. 验收标准
   - 功能完成后如何判断可以合并。
   - 需要运行哪些测试或手动验证。

没有 feature plan 的业务功能不应直接进入实现。

## 7. 质量门禁

开发和重构必须满足以下基础要求：

- 配置必须来自环境变量或配置系统，不硬编码密钥、令牌、环境地址。
- 数据库结构变化必须配套 Alembic migration。
- 涉及用户数据的接口必须有鉴权。
- 涉及用户数据的查询和修改必须按当前用户隔离。
- 涉及第三方服务的代码必须可 mock，测试不得依赖真实外部网络。
- 新增或改变行为时必须新增或更新测试。
- 错误处理必须返回统一响应结构。
- 日志不得输出密码、token、secret 或隐私敏感内容。
- 不在无明确计划的情况下实现 AI agents 具体能力。

## 8. 测试要求

默认使用 `pytest` 编写测试。

测试应优先覆盖：

- API 成功路径。
- 请求参数校验失败。
- 未登录、无效 token、权限不足。
- 用户之间的数据隔离。
- 数据库唯一约束和状态变更。
- 第三方服务超时、HTTP 错误、返回异常数据。

涉及外部服务时，应通过 mock 或 fake client 验证 server 行为。测试不应依赖真实 OIDC provider、词典服务、翻译服务或 agents 服务。

## 9. 数据库与迁移

数据库使用 PostgreSQL。

开发规则：

- ORM 模型和 Alembic migration 必须同步。
- 不依赖自动建表作为正常开发流程。
- 索引、唯一约束、外键和可空性应在 feature plan 中说明。
- 开发期如果需要重建干净 schema，应在计划中明确说明。
- 已有数据是否需要迁移，必须在对应 feature plan 中确认。

## 10. 认证与用户边界

认证系统属于 server 的核心基础设施。

默认方向：

- 使用 FastAPI Users 作为认证基础设施。
- 支持邮箱密码登录。
- 支持第三方 OAuth/OIDC 登录。
- 本地用户和第三方身份通过外部身份映射表关联。
- 第一阶段登录态优先使用 access token。

具体第三方 provider、token 生命周期、账号绑定规则、密码重置方式等，必须在认证功能的 feature plan 中单独确定。

## 11. 非 AI 功能边界

非 AI 功能主要包括不依赖 agents 项目的学习业务能力，例如：

- 词典查询。
- 翻译。
- 单词详情聚合。
- 用户词本。
- 复习记录。

这些功能可以调用外部词典或翻译服务，但不应直接写入 agents 逻辑。是否缓存、如何缓存、缓存失效策略、复习算法等，都应在对应 feature plan 中单独设计。

## 12. AI Agents 接入边界

server 可以作为 agents 项目的调用方，但不承载 agents 的内部实现。

原则：

- server 只定义稳定的客户端、鉴权、限流、错误映射和业务编排。
- agents 的提示词、模型选择、评测、工具调用由 `english-learning-agents` 项目负责。
- agents 接口未稳定前，server 只保留清晰的 integration 边界，不提前实现复杂业务。

## 13. 文档维护

当以下内容变化时，必须更新本文档或对应 feature plan：

- 核心技术栈。
- 项目职责边界。
- 目录分层和模块职责。
- API 统一约定。
- 数据库迁移策略。
- 测试和质量门禁。
- 认证、非 AI、AI agents 三大模块的边界。

本文档保持总体指导层级。具体功能细节应写入对应 feature plan，而不是不断塞进本文档。
